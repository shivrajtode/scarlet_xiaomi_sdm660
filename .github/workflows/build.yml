name: Build Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y bc bison flex libssl-dev libelf-dev cpio

      - name: Get clang toolchain
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang
          rm -f clang/bin/*ld*

      - name: Add KernelSU-Next (legacy, for old kernels)
        run: curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -s legacy

      - name: Patch defconfig with the allowlist fix
        run: echo "CONFIG_KSU_ALLOWLIST_WORKAROUND=y" >> arch/arm64/configs/vendor/tulip-perf_defconfig

      - name: Build
        run: |
          export PATH="$PWD/clang/bin:$PATH"
          make O=out ARCH=arm64 vendor/tulip-perf_defconfig
          set -o pipefail && make -j$(nproc) O=out ARCH=arm64 CC=clang CROSS_COMPILE=aarch64-linux-gnu- 2>&1 | tee build.log

      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-output
          path: |
            out/arch/arm64/boot/Image.gz-dtb
            out/arch/arm64/boot/Image
            build.log
          if-no-files-found: warn
